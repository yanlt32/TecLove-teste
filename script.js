function openDemo(demoId) {
    const demoModal = document.getElementById(demoId);
    demoModal.style.display = "flex";
}

function closeDemo(demoId) {
    const demoModal = document.getElementById(demoId);
    demoModal.style.display = "none";
}
